# Handle line endings automatically for text files
* text=auto

# Explicitly declare files that should always use LF (e.g., shell scripts)
*.sh text eol=lf

# Explicitly declare files that should always use CRLF (e.g., Windows batch files)
*.bat text eol=crlf
*.cmd text eol=crlf
